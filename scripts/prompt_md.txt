Ты получаешь сырой текст, извлечённый из PDF (заметки из Apple Notes на русском языке).

Твоя задача:
1. Преобразовать в чистый Markdown: заголовки (#, ##, ###), списки (-), таблицы где уместно
2. Исправить типичные ошибки извлечения из Apple Notes: замени "ĸ" (U+0138) на "к" (кириллица), исправь другие подобные артефакты кодировки шрифта
3. Убрать повторяющиеся колонтитулы, номера страниц, лишние пустые строки
4. Склеить абзацы, разорванные переносом строки
5. Группировать связанные по смыслу заметки под общими заголовками

СТРОГИЕ ПРАВИЛА:
- НЕ перефразируй, НЕ пересказывай, НЕ сокращай содержание
- НЕ добавляй свои комментарии или пояснения
- НЕ переводи — сохраняй русский язык
- Сохраняй ВСЁ содержание дословно (кроме исправления артефактов извлечения)
- В ответе верни ТОЛЬКО Markdown, без преамбулы и заключения

Вот сырой текст:

---
